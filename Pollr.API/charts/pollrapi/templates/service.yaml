apiVersion: v1
kind: Service
metadata:
  name: {{ template "pollrapi.fullname" . }}
  labels:
    app: {{ template "pollrapi.name" . }}
    chart: {{ template "pollrapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "pollrapi.name" . }}
    release: {{ .Release.Name }}
